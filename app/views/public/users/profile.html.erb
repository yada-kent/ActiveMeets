<div class="my-page-container">
  <h1><%= @user.name %> のプロフィール</h1>

  <div class="user-profile-image">
    <% if @user.avatar.attached? %>
      <%= image_tag @user.avatar, class: 'user-avatar' %>
    <% else %>
      <%= image_tag 'default_avatar.png', class: 'user-avatar' %> <!-- この部分はデフォルトのアイコンへのパスを指定してください -->
    <% end %>
  </div>

  <div class="user-info">
    <p><strong>名前:</strong> <%= @user.name %></p>
    <p><strong>プロフィール:</strong> <%= @user.introduction %></p>
  </div>

  <div class="follow-stats">
    <p>
      <%= link_to "#{@user.followees.count} フォロー中", user_followees_path(@user) %>
    </p>
    <p>
      <%= link_to "#{@user.followers.count} フォロワー", user_followers_path(@user) %>
    </p>
  </div>

  <% unless current_user == @user %>
    <% if current_user.followees.include?(@user) %>
      <%= button_to "フォロー解除", users_follow_path(followed_id: @user.id), method: :delete %>
    <% else %>
      <%= button_to "フォロー", users_follow_path(followed_id: @user.id), method: :post %>
    <% end %>
  <% end %>

  <% if current_user == @user %>
    <div class="user-actions">
      <%= link_to "プロフィール編集", edit_users_path, class: "btn btn-secondary" %>
      <%= link_to "いいね一覧", likes_users_path, class: "btn btn-info" %>
    </div>
  <% end %>
</div>
